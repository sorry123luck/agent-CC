# Core
pydantic>=2.0
SQLAlchemy>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29
typer>=0.12
loguru>=0.7

# Windows Automation
pywin32>=306
psutil>=5.9
pywinauto>=0.6
uiautomation>=2.0

# OCR / Vision
# 正式方案：PaddleOCR（中文识别精度高，是当前唯一正式的 OCR 方案）
paddleocr>=2.7
paddlepaddle>=2.5
opencv-python>=4.9
Pillow>=10.0

# 历史临时方案：EasyOCR（仅作开发对照，不进正式运行时）
#   ⚠️ 注意：在深色 GUI（如 VS Code）场景下识别质量严重不合格
#   不进入正式运行时
# easyocr>=1.7

# 降级方案：Tesseract（速度快，但深色 GUI 场景碎片化严重）
# Windows: pip install pytesseract && 安装 Tesseract-OCR
pytesseract>=0.3.13

# HTTP Client
httpx>=0.27
requests>=2.31

# Browser DOM Runtime
playwright>=1.52

# Utils
pyyaml>=6.0
pytest>=8.0
pytest-mock>=3.12
